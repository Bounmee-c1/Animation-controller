import 'package:flutter/material.dart';

class bounmee extends StatelessWidget {
  const bounmee({super.key});

  @override
  Widget build(BuildContext context) {
    return bounmee(
      body: Center(
        child: Icon(
          Icons.cake,
          size: 120.0,
          color: Colors.orange,
        ),
      ),
    );
  }
}
